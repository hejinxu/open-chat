# 嵌入式对话组件实现方案

> 状态: 已实施
> 日期: 2026-05-13
> 方案: iframe + postMessage 嵌入

## 概述

提供一段前端代码，让其他 Web 应用以浮动窗口方式集成 open-chat 对话功能。
交互逻辑：浮动图标（可拖动）→ 点击弹出对话窗口 → 关闭恢复图标。
所有 UI 控件（标题栏、☰、×、拖拽、resize）由外层 `embed.min.js` 管理，iframe 仅渲染纯对话内容。

## 最终架构

```
用户网站 (宿主页面)
  └── embed.min.js (在宿主 DOM 中，纯 vanilla JS)
       ├── [浮动按钮]      ← 宿主 DOM 中渲染，支持拖拽
       └── [窗口容器]      ← 宿主 DOM 中渲染
            ├── [标题栏]   ← ☰ + 标题文字 + ×，颜色由 headerStyle 配置
            ├── [iframe]   ← 加载 /embed?token=...&theme=...
            │    └── /embed 页面 (Next.js，复用 Main 组件的布局)
            └── [resize]   ← 右下角拖拽 handle，min/max 约束

控件归属:
  ☰ → 外层，通过 postMessage 发 toggle-sidebar 到 iframe
  × → 外层，直接 closeWindow()
  拖拽 → 外层标题栏
  resize → 外层右下角
  主题色 → 外层容器背景 + 标题栏颜色（headerStyle）
  iframe 内主题 → 通过 theme URL 参数传入 CSS 变量
```

## openChatConfig 配置接口

```javascript
window.openChatConfig = {
  // 必填
  baseUrl: 'http://localhost:3000',   // open-chat 服务器地址
  token: 'embed-token',               // API Token（匹配 embed_tokens 表）

  // 可选
  agentId: 'xxx',                     // 预设智能体
  icon: 'robot',                      // 内置图标名（14 个可选），默认 'robot'
  iconUrl: 'https://...',             // 自定义图标 URL（优先级高于 icon）
  windowTitle: 'AI 助手',             // 窗口标题栏文字
  theme: 'light',                     // light | dark | tech-blue
  locale: 'zh-Hans',                  // 语言

  // 窗口尺寸
  windowSize: {
    width: 400,          // 初始宽度(px)，默认 400
    height: 600,         // 初始高度(px)，默认 600
    minWidth: 320,       // 最小宽度，默认 320
    minHeight: 400,      // 最小高度，默认 400
    maxWidth: 800,       // 最大宽度，默认 800
    maxHeight: 900,      // 最大高度，默认 900
  },

  // 标题栏样式
  headerStyle: {
    backgroundColor: '#1C64F2',         // 标题栏背景，不填按主题取默认值
    borderColor: 'rgba(255,255,255,0.2)',  // 标题栏底边颜色
    textColor: '#ffffff',              // 标题文字颜色
    iconColor: '#ffffff',              // ☰ × 图标颜色
    buttonHoverBg: 'rgba(255,255,255,0.15)',  // 按钮悬停背景
  },

  // 浮动按钮样式
  bubbleStyle: {
    backgroundColor: '#1C64F2',        // 按钮背景色（无 iconUrl 时）
  },

  // 预设参数（填入智能体的 prompt_variables）
  inputs: { key: 'value' },
}
```

## 数据模型

### embed_tokens 表（存入现有 openchat.db）

```sql
CREATE TABLE IF NOT EXISTS embed_tokens (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT DEFAULT '',
  token TEXT NOT NULL UNIQUE,
  allowed_agent_ids TEXT NOT NULL DEFAULT '[]',  -- JSON 字符串数组，'*' 表示全部
  is_enabled INTEGER NOT NULL DEFAULT 1,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
)
```

### TypeScript 类型

```typescript
interface EmbedTokenRecord {
  id: string
  name: string
  description: string
  token: string
  allowed_agent_ids: string[]
  is_enabled: boolean
  created_at: number
  updated_at: number
}
```

## Token 校验流程

```
API 请求到达 /api/chat-messages (或其他 API)
  ↓
读取 x-embed-token header
  ↓ 无 → 跳过校验（现有模式不受影响）
  ↓ 有
DatabaseProvider.getEmbedTokenByValue(token)
  ↓ 找不到 → 401 Unauthorized
  ↓ 找到
检查 is_enabled === true
  ↓ false → 403 Token disabled
  ↓ true
检查 agentId in allowed_agent_ids (或 '*')
  ↓ 否 → 403 Agent not allowed
  ↓ 是 → 放行
```

## 功能范围

嵌入窗口包含完整功能：
- 智能体选择器
- 对话历史侧边栏（☰ 按钮在外层标题栏触发）
- 语音输入 (browser + whisper)
- 文件/图片上传
- 主题切换

## 文件清单

| # | 文件 | 操作 | 说明 |
|---|------|------|------|
| **数据库 & 认证** | | | |
| 1 | `webapp/types/embed.ts` | 新建 | `EmbedTokenRecord` 类型定义 |
| 2 | `webapp/lib/db/types.ts` | 修改 | `DatabaseProvider` 接口增加 5 个 embed token 方法 |
| 3 | `webapp/lib/db/sqlite.ts` | 修改 | 建 `embed_tokens` 表 + CRUD 实现 |
| 4 | `webapp/app/api/utils/embed-auth.ts` | 新建 | `validateEmbedToken(token, agentId)` 校验函数 |
| 5 | `webapp/app/api/utils/common.ts` | 修改 | 新增 `checkEmbedToken()` + `getAdapterForRequest` 调用校验 |
| **API 路由** | | | |
| 6 | `webapp/app/api/chat-messages/route.ts` | 修改 | `await getAdapterForRequest` |
| 7 | `webapp/app/api/chat-messages/[taskId]/stop/route.ts` | 修改 | 调用 `checkEmbedToken` |
| 8 | `webapp/app/api/conversations/route.ts` | 修改 | 调用 `checkEmbedToken` |
| 9 | `webapp/app/api/conversations/[conversationId]/name/route.ts` | 修改 | 调用 `checkEmbedToken` |
| 10 | `webapp/app/api/file-upload/route.ts` | 修改 | `await getAdapterForRequest` |
| 11 | `webapp/app/api/messages/route.ts` | 修改 | 调用 `checkEmbedToken` |
| 12 | `webapp/app/api/messages/[messageId]/feedbacks/route.ts` | 修改 | 调用 `checkEmbedToken` |
| 13 | `webapp/app/api/parameters/route.ts` | 修改 | `await getAdapterForRequest` |
| **嵌入页面** | | | |
| 14 | `webapp/app/embed/layout.tsx` | 新建 | 嵌入版布局 |
| 15 | `webapp/app/embed/page.tsx` | 新建 | 嵌入页面入口（读取 URL 参数、应用 theme） |
| 16 | `webapp/app/embed/main-embed.tsx` | 新建 | 嵌入包装（纯 Main 渲染，无标题栏） |
| 17 | `webapp/app/embed/embed-config.tsx` | 新建 | `EmbedConfig` 上下文 + `EmbedConfigProvider` |
| **主组件** | | | |
| 18 | `webapp/app/components/index.tsx` | 修改 | `isEmbed`/`embedToken` 从 props 读取；监听 `com.openchat.embed` toggle-sidebar |
| 19 | `webapp/service/index.ts` | 修改 | `sendChatMessage` 透传 `x-embed-token` header |
| **嵌入兼容修复** | | | |
| 20 | `webapp/app/components/chat/voice-settings.tsx` | 修改 | 面板从 absolute 改为 fixed 定位，解决 iframe 内 overflow:hidden 裁切问题 |
| **前端脚本** | | | |
| 21 | `webapp/public/embed.min.js` | 新建 | 前端嵌入脚本（纯 JS, ~450 行） |
| **图标资源** | | | |
| 22 | `webapp/public/images/embed-icons/*.svg` | 新建 | 14 个内置图标（robot/chat/sparkle/headset/message/bot/brain/wand/rocket/puzzle/eye/code/gear） |
| **测试 & 工具** | | | |
| 23 | `webapp/scripts/seed-embed-tokens.mjs` | 新建 | Token seed 脚本 |
| 24 | `embed-test/` | 新建 | pnpm workspace 测试工程 |
| **根配置** | | | |
| 25 | `pnpm-workspace.yaml` | 修改 | 添加 `embed-test` |
| 26 | `package.json` | 修改 | 添加 `dev:embed-test` 脚本 |

## 现有模式兼容性

三条防线确保零影响：
1. **数据层** — `CREATE TABLE IF NOT EXISTS`，不修改旧表结构
2. **认证层** — `if (embedToken)` 守卫，现有请求无此 header 则完全跳过校验
3. **组件层** — `isEmbed` 默认 `false`，渲染路径与现在完全一致

## 后续集成

未来登录系统上线时，在 middleware 中添加白名单：

```typescript
// 登录中间件
if (pathname.startsWith('/embed') || request.headers.get('x-embed-token')) {
  return NextResponse.next() // 跳过 JWT 验证
}
```
